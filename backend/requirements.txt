# ── FastAPI ────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.1
python-multipart==0.0.20

# ── Database ───────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.38
asyncpg==0.30.0                   # Async PostgreSQL driver
psycopg2-binary==2.9.10           # Required by LlamaIndex for Postgres
pgvector==0.3.6                   # Python support for vector type
aiomysql                          # Async MySQL driver
cryptography                      # Required for MySQL auth plugin

# ── LLM APIs ──────────────────────────────────────────────────
google-generativeai>=0.5.4        # Gemini Pro (Old SDK, needed by LlamaIndex)
google-genai                      # Modern SDK (New, used by our Agents)
groq                              # Groq API (Router, Analyst, QA, Refiner, Python agents)
openai>=1.30.0                    # GitHub Models fallback (OpenAI-compatible API via Azure AI)
httpx==0.28.1                     # HTTP client

# ── LlamaIndex + pgvector ─────────────────────────────────────
llama-index-core==0.12.14
llama-index-vector-stores-postgres==0.4.0
llama-index-embeddings-gemini>=0.3.0
llama-index-llms-gemini

# ── Data Science (Python Analyst Agent) ───────────────────────
pandas                            # DataFrame processing in sandbox
numpy                             # Numerical computation in sandbox

# ── Embeddings ────────────────────────────────────────────────
sentence-transformers==3.4.1
torch==2.6.0                      # CPU build — change for GPU

# ── Caching ───────────────────────────────────────────────────
redis[asyncio]==5.2.1

# ── Celery (background tasks) ─────────────────────────────────
celery==5.4.0
flower==2.0.1                     # Celery monitoring UI

# ── File Processing (Document Indexer) ────────────────────────
pypdf                             # PDF file reading

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1
